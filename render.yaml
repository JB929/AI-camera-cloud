services:
  - type: web
    name: ai-camera-cloud
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn src.dashboard_server.main:app --host 0.0.0.0 --port $PORT

